{
  "name": "meteor-auth0-lock",
  "version": "0.1.0",
  "description": "Use the Auth0 lock widget to log into a meteor app from another origin (e.g. landing page)",
  "main": "lib/index.js",
  "files": [
      "lib/index.js"      
  ],
  "scripts": {
    "test": "test",
    "lint": "npx prettier --write src",
    "start": "parcel src/index.html -p 3010",
    "sandbox": "rm -r lib && parcel build src/index.js -d sandbox --target browser --no-source-maps",
    "build": "rm -r lib && parcel build src/index.js -d lib --target browser --no-source-maps"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/optune/meteor-auth0-lock.git"
  },
  "keywords": [
    "meteor",
    "auth0",
    "lock",
    "oauth"
  ],
  "author": "Joël Meiller",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/optune/meteor-auth0-lock/issues"
  },
  "homepage": "https://github.com/optune/meteor-auth0-lock#readme",
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.1.1"
  }
}
